export default `
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link
} from "react-router-dom"

const Routes = () => {
  return (
    <Router>
        <Switch>
        </Switch>
    </Router>
  )
}

export default Routes
`
